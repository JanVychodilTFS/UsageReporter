{
  // Rename this file to config.json and fill in your values before use.
  "UserEmail": "your.email@example.com",
  // Where the tool is installed; %LOCALAPPDATA% expands to C:\Users\<you>\AppData\Local
  "InstallationPath": "%LOCALAPPDATA%\\UsageReporter",
  // Codex settings directory used by ccusage; %USERPROFILE% avoids hardcoding a user name.
  "CodexSettingsPath": "%USERPROFILE%\\.codex",
  // Include archived Codex sessions by setting CODEX_HOME only for the ccusage subprocess.
  "ReadArchivedSessions": false,
  "Automation": 
  {
    "Enabled": true,
    // Cron expression - default: every Monday at 06:00
    "Schedule": "0 6 * * 1",
    // HTTP POST endpoint that receives {"data": [report rows]}
    "TargetURL": "https://your-target-url-here",
    // Each entry produces one report; Agent: "codex" | QueryType: "lastWeek" | "yesterday" | "yesterdaySessions"
    "Data": [
      {
        "Agent": "codex",
        "QueryType": "lastWeek"
      }
    ]
  }
}
